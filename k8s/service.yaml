apiVersion: v1
kind: Service
metadata:
  name: miniflux-ai
  labels:
    app: miniflux-ai
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: miniflux-ai
